#!/bin/sh
set -eu

bin_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
if [ -n "${JAVA_HOME:-}" ]; then
    exec "$JAVA_HOME/bin/java" -jar "$bin_dir/../lib/asyncapi-generator.jar" "$@"
fi
exec java -jar "$bin_dir/../lib/asyncapi-generator.jar" "$@"
