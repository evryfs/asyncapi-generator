asyncapi: 3.0.0
info:
  title: Broken Channel Ref
  version: 1.0.0
channels: {} # No channels defined
operations:
  testOperation:
    action: send
    channel:
      $ref: '#/channels/nonExistentChannel' # This channel does not exist
    messages:
      - $ref: '#/components/messages/myMessage' # Assume this exists for the test
components:
  messages:
    myMessage:
      payload:
        type: string
