asyncapi: 3.0.0
info:
  title: Invalid Operation Action
  version: 1.0.0
channels:
  myChannel:
    messages:
      myMessage:
        payload:
          type: string
components:
  messages: # Define the message here for reference
    UserSignedUp:
      payload:
        type: object
operations:
  testOperation:
    action: " send " # Exact value is not normalized to 'send'
    channel:
      $ref: '#/channels/myChannel'
    messages:
      - $ref: '#/channels/myChannel/messages/myMessage'
