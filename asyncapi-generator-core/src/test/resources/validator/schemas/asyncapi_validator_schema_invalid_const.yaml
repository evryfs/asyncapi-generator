asyncapi: 3.0.0
info:
  title: Invalid Const Value Test
  version: 1.0.0
components:
  schemas:
    StringTypeWithNumericConst:
      type: string
      const: 123 # Invalid: string type with numeric const

    NumberTypeWithStringConst:
      type: number
      const: "abc" # Invalid: number type with string const

    BooleanTypeWithStringConst:
      type: boolean
      const: "true" # Invalid: boolean type with string "true" (should be boolean true)
