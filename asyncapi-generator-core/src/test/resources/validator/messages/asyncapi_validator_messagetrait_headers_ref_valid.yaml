asyncapi: 3.0.0
info:
  title: Message Trait Headers Ref Valid
  version: 1.0.0
components:
  messageTraits:
    DefaultKafkaHeaders:
      contentType: application/json; charset=utf-8
      headers:
        $ref: "#/components/schemas/DefaultHeaders"
      examples:
        - payload: null
    DocumentedTrait:
      description: Documentation is meaningful trait content
  schemas:
    DefaultHeaders:
      type: object
      required:
        - traceparent
      properties:
        traceparent:
          type: string
          description: Trace context identifier
