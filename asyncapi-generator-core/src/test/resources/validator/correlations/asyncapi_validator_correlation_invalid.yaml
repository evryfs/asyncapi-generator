asyncapi: 3.0.0
info:
  title: Invalid Correlation ID (Warnings)
  version: 1.0.0
channels: {}
components:
  correlationIds:
    # Case 1: Invalid Location Regex (Warning)
    InvalidLocationRegex:
      location: "not_a_runtime_expression" # Will trigger a warning
      description: "Valid description"

    # Case 2: Short Description (Warning)
    ShortDescription:
      location: "$message.header#/id"
      description: "hi" # Will trigger a warning

