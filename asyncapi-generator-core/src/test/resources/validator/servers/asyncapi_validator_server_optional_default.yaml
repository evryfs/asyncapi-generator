asyncapi: 3.0.0
info:
  title: Optional server variable default
  version: 1.0.0
servers:
  production:
    host: '{environment}.api.example.com'
    protocol: https
    variables:
      environment:
        enum:
          - production
