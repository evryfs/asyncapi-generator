asyncapi: '3.0.0'
info:
  title: Ambiguity Test
  version: '1.0.0'
channels:
  userUpdates:
    address: 'users.updates'
    messages:
      messageOne:
        $ref: '#/components/messages/UserUpdate'
      messageTwo:
        $ref: '#/components/messages/UserUpdate'
components:
  messages:
    UserUpdate:
      payload:
        type: string
