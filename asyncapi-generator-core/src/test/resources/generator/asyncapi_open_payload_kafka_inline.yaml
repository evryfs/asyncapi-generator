asyncapi: 3.0.0
info:
  title: Open Payload Kafka
  version: 1.0.0
channels:
  user.raw-events:
    messages:
      RawEvent:
        name: Raw Event
        payload:
          type: object
          additionalProperties: true
