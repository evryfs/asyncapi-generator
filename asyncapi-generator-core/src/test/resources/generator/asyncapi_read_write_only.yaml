asyncapi: 3.0.0
info:
  title: Read Write Only Test
  version: 1.0.0
components:
  schemas:
    AccessControlledObject:
      type: object
      properties:
        id:
          type: string
          description: A read-only identifier.
          readOnly: true
        password:
          type: string
          description: A write-only password field.
          writeOnly: true
        status:
          type: string
          description: A regular read-write field.
        secretKey:
          type: string
          writeOnly: true
          readOnly: true # This is allowed in spec, typically means it's internal/system-managed.
