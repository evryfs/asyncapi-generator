asyncapi: 3.0.0
info:
  title: String Default Value Test
  version: 1.0.0
components:
  schemas:
    UserSignedUp:
      type: object
      properties:
        userId:
          type: string
          default: "myString"
        email:
          type: string
          format: email
        createdAt:
          type: string
          format: date-time
        referralCode:
          type: string
      required: [userId, email, createdAt]
