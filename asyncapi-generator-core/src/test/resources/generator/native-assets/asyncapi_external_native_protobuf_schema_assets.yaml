asyncapi: '3.0.0'
info:
  title: External Native Protobuf Schema Assets Generator Test API
  version: '1.0.0'
channels:
  userEvents:
    address: user.events
    messages:
      userCreatedProtobuf:
        name: UserCreatedProtobuf
        payload:
          $ref: '#/components/schemas/UserCreatedProtobuf'
components:
  schemas:
    UserCreatedProtobuf:
      schemaFormat: application/vnd.google.protobuf;version=3
      schema:
        $ref: user-created.proto
