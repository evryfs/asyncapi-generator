asyncapi: 3.0.0
info:
  title: Open Payload Kafka
  version: 1.0.0
channels:
  user.dlq:
    messages:
      DlqEvent:
        name: DlqPayload
        payload:
          $ref: '#/components/schemas/DlqPayload'
components:
  schemas:
    DlqPayload:
      type: object
      additionalProperties: true
