asyncapi: 3.0.0
info:
  title: Open Payload Kafka
  version: 1.0.0
channels:
  user.raw-events:
    messages:
      RawEvent:
        name: Raw Event
        payload:
          $ref: '#/components/schemas/RawEvent'
components:
  schemas:
    RawEvent:
      type: object
      additionalProperties: true
