asyncapi: 3.0.0
info:
  title: AnyOf Composition Example
  version: 1.0.0
defaultContentType: application/json
components:
  schemas:
    NameOrId:
      description: Value may be an ID or a name.
      anyOf:
        - type: string
          pattern: "^[A-Za-z_][A-Za-z0-9_]{0,30}$"
        - type: integer
          minimum: 1
