asyncapi: 3.0.0
info:
  title: Invalid Operation Parser Test
  version: 1.0.0
channels: {}
operations:
  MissingAction:
    channel:
      $ref: '#/channels/myChannel'
operationCases:
  MissingChannel:
    invalidOperation:
      action: send
