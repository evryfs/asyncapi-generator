asyncapi: 3.0.0
info:
  title: Invalid Security Parser Test
  version: 1.0.0
channels: {}
components:
  securitySchemes:
    # Case 1: Missing Type (Parser Error)
    MissingType:
      description: "No type provided"

    # Case 2: Invalid Flows Structure (Parser Error)
    InvalidFlowsStructure:
      type: oauth2
      flows: "not-an-object"
