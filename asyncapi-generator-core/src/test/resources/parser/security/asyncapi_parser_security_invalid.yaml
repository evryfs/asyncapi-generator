asyncapi: 3.0.0
info:
  title: Invalid Security Parser Test
  version: 1.0.0
channels: {}
components:
  securitySchemes:
    MissingType:
      description: "No type provided"

    MissingAvailableScopes:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://example.com/authorize
          tokenUrl: https://example.com/token
