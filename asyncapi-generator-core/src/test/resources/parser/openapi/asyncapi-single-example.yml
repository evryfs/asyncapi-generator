asyncapi: 3.0.0
info:
  title: Something
  version: 1.0.0
  tags:
    - name: "Something"

channels:
  someEvents:
    messages:
      EventMessage:
        $ref: '#/components/messages/EventMessage'
components:
  messages:
    EventMessage:
      name: EventMessage
      payload:
        type: object
        properties:
          actionType:
            $ref: './openapi-example.yml#/components/schemas/ActionType'