openapi: 3.0.0
info:
  title: example-common
  version: 0.0.1
  description: something
servers:
  - url: 'http://localhost:8080'
paths: { }
components:
  schemas:
    ActionType:
      title: ActionType
      enum:
        - CREATE
        - UPDATE
        - DELETE