asyncapi: 3.0.0
info:
  title: Recursive external schema name collision
  version: 1.0.0
channels:
  firstNodeEvents:
    address: first.node.events
    messages:
      firstNodeUpdated:
        name: FirstNodeUpdated
        payload:
          $ref: './first-node.yaml#/components/schemas/SharedNode'
  secondNodeEvents:
    address: second.node.events
    messages:
      secondNodeUpdated:
        name: SecondNodeUpdated
        payload:
          $ref: './second-node.yaml#/components/schemas/SharedNode'
