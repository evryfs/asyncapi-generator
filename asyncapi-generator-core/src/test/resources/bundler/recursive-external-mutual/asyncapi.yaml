asyncapi: 3.0.0
info:
  title: Mutually recursive external schema bundling
  version: 1.0.0
channels:
  relationshipEvents:
    address: relationship.events
    messages:
      relationshipUpdated:
        name: RelationshipUpdated
        payload:
          $ref: './schemas.yaml#/components/schemas/ParentNode'
