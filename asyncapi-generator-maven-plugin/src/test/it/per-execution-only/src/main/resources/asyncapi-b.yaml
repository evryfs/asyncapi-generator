asyncapi: 3.0.0
info:
  title: Test B
  version: 1.0.0
channels:
  somethingUpdated:
    address: test.topic.b.v1
    messages:
      - $ref: '#/components/messages/SomethingUpdated'
components:
  messages:
    SomethingUpdated:
      name: SomethingUpdated
      title: SomethingUpdated
      payload:
        $ref: '#/components/schemas/SomethingUpdatedEvent'
  schemas:
    SomethingUpdatedEvent:
      type: object
      properties:
        id:
          type: string
      required:
        - id
